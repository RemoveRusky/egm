{
if instance_exists(obj_point) // So that the time record doesn't change
second+=1 //Adds one second to the timer
if second>59 {minute+=1} // So that after 60 seconds,there is a minute added.
alarm[0]=room_speed //Repeats the same event after a second

/**/
}
